<IfModule mod_rewrite.c>
    RewriteEngine On

    # Redirigir todo lo que no sea un archivo o directorio real a app.php
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^ app.php [QSA,L]
</IfModule>
